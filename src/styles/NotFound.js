import { makeStyles } from '@mui/styles';

const useStyles = makeStyles({
  root: {
    display: 'flex',
    flex: 1,
    // alignItems:'center'
    paddingTop:'200px'
  },
  contents: {
    cursor:'default',
    fontSize:'24px',
  }

});
export default useStyles;